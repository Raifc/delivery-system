<h1 class="text-center display-4 bg-dark text-white"> <%= Company.model_name.human %> <%= @company.trading_name %></h1>

<div class="d-flex justify-content-center">
  <%= render @company %>
</div>

<div class="d-flex justify-content-center">
  <% if admin_signed_in? %>
     <%= link_to "Editar transportadora", edit_company_path(@company), :class => "btn btn-primary mt-3 mx-2" %>
     <%= link_to "Ordens de Serviço", company_service_orders_path(@company), :class => "btn btn-primary mt-3" %>
  <% end %>

</div>
